function Detail({ data }) {
  return (
    <div>
      <h1>DETAIL</h1>
    </div>
  );
}

// export async function getServerSideProps() {
//   const res = await fetch('https://covid-19-api-nine.vercel.app/api');
//   const data = await res.json();

//   return { props: { data } };
// }

export default Detail;
